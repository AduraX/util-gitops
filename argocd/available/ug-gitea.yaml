apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ug-gitea
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: util-gitops
    util-gitops.io/category: devtools
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: ug-infra
  sources:
    - repoURL: https://dl.gitea.com/charts/
      chart: gitea
      targetRevision: "*"
      helm:
        releaseName: gitea
        valueFiles:
          - $values/platform/devtools/gitea/values.yaml
    - repoURL: https://gitea.util.lcl/AduraX/util-gitops.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: devtools
  ignoreDifferences:
    - group: ""
      kind: Service
      jsonPointers:
        - /spec/clusterIP
        - /spec/clusterIPs
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=true
