apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ug-openldap
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: util-gitops
    util-gitops.io/category: identity
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: ug-infra
  sources:
    - repoURL: https://jp-gouin.github.io/helm-openldap/
      chart: openldap-stack-ha
      targetRevision: "*"
      helm:
        releaseName: openldap
        valueFiles:
          - $values/platform/identity/openldap/values.yaml
    - repoURL: https://gitea.util.lcl/AduraX/util-gitops.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: identity
  ignoreDifferences:
    - group: ""
      kind: Service
      jsonPointers:
        - /spec/clusterIP
        - /spec/clusterIPs
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=false
