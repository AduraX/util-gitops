# OpenBao Helm values (OSS Vault fork, MPL-2.0)
# Chart: openbao/openbao

server:
  ha:
    enabled: false
  dataStorage:
    enabled: true
    size: 1Gi
  ui:
    enabled: true
    serviceType: ClusterIP
