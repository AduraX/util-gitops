# Gitea Helm values
# Chart: gitea/gitea

gitea:
  admin:
    username: admin
    password: changeme
    email: admin@gitops.lcl
  config:
    APP_NAME: Gitea
    server:
      DOMAIN: gitea.gitops.lcl
      ROOT_URL: https://gitea.gitops.lcl
      SSH_DOMAIN: gitea.gitops.lcl
      SSH_PORT: 22
    repository:
      ENABLE_PUSH_CREATE_USER: true
      ENABLE_PUSH_CREATE_ORG: true
    actions:
      ENABLED: true
    database:
      DB_TYPE: postgres
      HOST: gitea-db-rw.storage.svc.cluster.local:5432
      NAME: gitea
      USER: gitea
      PASSWD: changeme    # Override via --set at bootstrap
      SSL_MODE: disable

postgresql:
  enabled: false

postgresql-ha:
  enabled: false

persistence:
  enabled: true
  size: 10Gi

service:
  ssh:
    type: NodePort
    nodePort: 30022
